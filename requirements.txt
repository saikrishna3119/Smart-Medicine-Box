# requirements.txt — Smart Medicine Box
# Install with: pip install -r requirements.txt --break-system-packages

lgpio>=0.2.2.0
flask>=3.0.0
SpeechRecognition>=3.10.0
pyttsx3>=2.90
schedule>=1.2.1
PyAudio>=0.2.14
